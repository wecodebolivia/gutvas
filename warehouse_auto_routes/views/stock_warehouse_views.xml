<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Warehouse Form View Extension -->
    <record id="view_warehouse_form_inherit" model="ir.ui.view">
        <field name="name">stock.warehouse.form.inherit</field>
        <field name="model">stock.warehouse</field>
        <field name="inherit_id" ref="stock.view_warehouse"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet" position="before">
                <header>
                    <button name="action_regenerate_routes" 
                            string="Regenerate Routes" 
                            type="object" 
                            class="oe_highlight"
                            invisible="not active"/>
                </header>
            </xpath>
            <xpath expr="//field[@name='active']" position="after">
                <field name="auto_routes_generated" readonly="1"/>
            </xpath>
        </field>
    </record>

</odoo>
