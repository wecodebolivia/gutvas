<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Heredamos la vista de account.move -->
    <record id="view_move_form_inherit_libro_compras" model="ir.ui.view">
        <field name="name">account.move.form.inherit.libro.compras</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">

            <!-- Insertar el botón dentro del header -->
<header position="inside">
    <button name="action_open_libro_compras_wizard"
            type="object"
            string="Generar Libro de Compras"
            class="btn-primary"
            invisible="move_type != 'in_invoice'"/>
</header>


        </field>
    </record>
</odoo>
