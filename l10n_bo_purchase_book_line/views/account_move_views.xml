<odoo>
    <!-- Herencia de la vista de factura para agregar botón del libro de compras -->
    <record id="view_move_form_inherit_libro_compras" model="ir.ui.view">
        <field name="name">account.move.form.inherit.libro.compras</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">

            <!-- Agregar el botón en el header del formulario -->
            <xpath expr="//header" position="inside">
                <button
                    name="action_open_libro_compras_wizard"
                    type="object"
                    string="Libro de Compras"
                    class="btn-primary"
                    icon="fa-book"
                    groups="account.group_account_invoice"
                    invisible="move_type not in ('in_invoice', 'in_refund')"
                />
            </xpath>

        </field>
    </record>
</odoo>
